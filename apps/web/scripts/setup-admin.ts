/**
 * Setup admin user — delegates to apps/api.
 * Run: cd apps/api && ADMIN_EMAIL=you@example.com ADMIN_PASSWORD=secret npm run setup-admin
 */
console.log("Run setup-admin from apps/api:");
console.log("  cd apps/api && ADMIN_EMAIL=you@example.com ADMIN_PASSWORD=secret npm run setup-admin");
process.exit(0);
